services:
  - type: web
    name: sudan-quality-backend
    runtime: nodejs20
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
